# Empty `pages` folder

Эта папка оставлена пустой намеренно для совместимости с Next.js.
Когда используется App Router из корневой `app/`, Next пытается найти `pages` в `src` и может привести к конфликту. Наличие этой папки и этого README.md предотвращает такие проблемы.
